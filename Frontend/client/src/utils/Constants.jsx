export function createData(title, country, budget, destinations) {
  return {
    title,
    country,
    budget,
    destinations,
  };
}

// export function createData(destination) {
//     return {
//         destination
//     };
// }
